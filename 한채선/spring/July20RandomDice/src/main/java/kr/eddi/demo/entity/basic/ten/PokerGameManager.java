package kr.eddi.demo.entity.basic.ten;

import kr.eddi.demo.utility.ten.BettingMoneyManager;
import lombok.Getter;

@Getter
public class PokerGameManager {
/*
    private PlayerManager playerManager;
    private BettingMoneyManager bettingMoneyManager;
    private RoundManager roundManager;

    private PokerCardManager pokerCardManager;

    private final int DEFAULT_BETTING_MONEY = 1000000;

    public PokerGameManager(int playerNum) {
        playerManager = new PlayerManager(playerNum);
        roundManager = new RoundManager();
    }

    public void startGame() {
        //초기 배팅 머니 부과
        bettingMoneyManager.betMoney(DEFAULT_BETTING_MONEY, playerManager.getPlayerNum());

        do {
            roundManager.playGame(playerManager, pokerCardManager);
        } while(isGameFinish());
    }

    public Player decisionWinner() {
        return ;
    }

    public boolean isGameFinish() {

        return false;
    }*/
}
